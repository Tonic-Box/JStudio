package com.tonic.ui.query.ast;

/**
 * Query target type - what kind of entities the query returns.
 */
public enum Target {
    METHODS,
    CLASSES,
    PATHS,
    EVENTS,
    STRINGS,
    OBJECTS
}
